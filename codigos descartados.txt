#def load_user(idusuario):
    #return Usuario.query.filter_by(idusuario=idusuario).first()

@login_manager.user_loader
def get_user(user_id):
    return Usuario.query.filter_by(idusuario=user_id)



     {% if  current_user.is_authenticated %}
                        <li class="nav-item"><a href="{{ url_for('logout') }}" class="nav-link">Sair</a></li>
                        {% else %}
                        <li class="nav-item"><a href="{{ url_for('login') }}" class="nav-link">Entrar</a></li>
                        {% endif %}  




@property
def is_authenticated(self):
    return True

@property
def is_active(self):
    return True

@property
def is_anonymous(self):
    return False

def get_id(self):
    return (self.id)



@login_manager.user_loader
def load_user(idusuario):
    return Usuario.query.get(idusuario)


123                                          